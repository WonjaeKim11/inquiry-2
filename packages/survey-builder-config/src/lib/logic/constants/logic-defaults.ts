/** 조건 그룹 최대 중첩 깊이 */
export const MAX_NESTING_DEPTH = 10;

/** Block 최대 재방문 횟수 (무한 루프 방지) */
export const MAX_BLOCK_REVISIT = 100;

/** 로직 아이템 최대 개수 (Block 당) */
export const MAX_LOGIC_ITEMS_PER_BLOCK = 50;

/** 조건 그룹 내 최대 조건 수 */
export const MAX_CONDITIONS_PER_GROUP = 20;

/** 로직 아이템 당 최대 액션 수 */
export const MAX_ACTIONS_PER_ITEM = 10;
