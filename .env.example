DATABASE_URL="postgresql://inquiry:inquiry_password@localhost:5432/inquiry?schema=public"

# JWT
JWT_ACCESS_SECRET="your-access-secret-key"
JWT_REFRESH_SECRET="your-refresh-secret-key"

# OAuth: Google
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# OAuth: GitHub
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Client URL (OAuth 콜백 리다이렉트용)
CLIENT_URL="http://localhost:4200"

# SMTP (이메일 기능 활성화 시 필수)
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_USER="your-smtp-user"
# SMTP_PASSWORD="your-smtp-password"
# EMAIL_FROM="noreply@example.com"

# Cloudflare Turnstile CAPTCHA (선택)
# TURNSTILE_SECRET_KEY="your-turnstile-secret-key"

# Brevo 마케팅 연동 (선택)
# BREVO_API_KEY="your-brevo-api-key"

# Redis (Rate Limiting, 캐시 등)
# REDIS_URL="redis://localhost:6379"
# RATE_LIMIT_DISABLED=false        # true로 설정 시 Rate Limiting 비활성화

# Sentry 에러 추적 (선택)
# SENTRY_DSN="your-sentry-dsn"

# 로그 레벨 (debug, info, warn, error)
# LOG_LEVEL="info"

# License 키 (선택)
# LICENSE_KEY="your-license-key"

# 초대 JWT (초대 토큰 서명용, 미설정 시 JWT_ACCESS_SECRET 사용)
# JWT_INVITE_SECRET="your-invite-secret-key"

# 암호화 (AES-256-GCM, 2FA Secret/Backup Code 암호화에 사용)
# ENCRYPTION_KEY=                  # 32 bytes hex (64자). 미설정 시 평문 모드

# SSO: Azure AD (선택)
# AZURE_AD_CLIENT_ID=
# AZURE_AD_CLIENT_SECRET=
# AZURE_AD_TENANT_ID=

# SSO: OpenID Connect (선택)
# OPENID_CLIENT_ID=
# OPENID_CLIENT_SECRET=
# OPENID_ISSUER=

# CORS 추가 허용 도메인 (쉼표 구분, Client API/Management API 외부 접근용)
# CORS_ALLOWED_ORIGINS="https://app.example.com,https://sdk.example.com"

# 파일 저장 경로 (기본: ./uploads)
# STORAGE_PATH="./uploads"

# 기능 토글
# MULTI_ORG_ENABLED=true          # 자동 조직 생성 (기본: true)
# EMAIL_VERIFICATION_DISABLED=false # 이메일 검증 생략 (기본: false)
# SESSION_MAX_DURATION=86400       # JWT 세션 최대 시간 초 (기본: 86400)
# INVITE_DISABLED=false            # 초대 기능 비활성화 (기본: false)
# ROLE_MANAGEMENT_ENABLED=false    # 역할 관리 기능 활성화 (기본: false, Enterprise)
# IS_SELF_HOSTED=false             # Self-hosted 환경 여부 (기본: false)
# FEATURE_TWO_FACTOR_AUTH=false    # 2FA 기능 활성화 (기본: false)
# FEATURE_SSO=false                # SSO 기능 활성화 (기본: false)
# FEATURE_SAML=false               # SAML 기능 활성화 (기본: false)
